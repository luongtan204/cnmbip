<!DOCTYPE html>
<html lang="en">
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Sửa Sách</title>
</head>
<body class="container mt-4">
    <h2 class="mb-4">Sửa Sách: <%= book.title || '' %></h2>

    <% if (errors && errors.length > 0) { %>
        <div class="alert alert-danger">
            <ul class="mb-0">
                <% errors.forEach((e) => { %>
                    <li><%= e %></li>
                <% }) %>
            </ul>
        </div>
    <% } %>

    <form action="/edit/<%= book.bookId %>" method="POST" enctype="multipart/form-data" class="card p-4 shadow-sm">
        <input type="hidden" name="oldCoverImageUrl" value="<%= book.coverImageUrl || '' %>">

        <div class="row mb-3">
            <div class="col">
                <label class="form-label">Mã sách (bookId)</label>
                <input type="text" name="bookId" class="form-control" value="<%= book.bookId || '' %>" readonly>
            </div>
            <div class="col">
                <label class="form-label">Tiêu đề</label>
                <input type="text" name="title" class="form-control" placeholder="Nhập tiêu đề sách" value="<%= book.title || '' %>" required>
            </div>
        </div>

        <div class="row mb-3">
            <div class="col">
                <label class="form-label">Tác giả</label>
                <input type="text" name="author" class="form-control" placeholder="Nhập tên tác giả" value="<%= book.author || '' %>" required>
            </div>
            <div class="col">
                <label class="form-label">Thể loại</label>
                <select name="category" class="form-select" required>
                    <option value="Tiểu thuyết" <%= book.category === 'Tiểu thuyết' ? 'selected' : '' %>>Tiểu thuyết</option>
                    <option value="CNTT" <%= book.category === 'CNTT' ? 'selected' : '' %>>CNTT</option>
                    <option value="Kinh tế" <%= book.category === 'Kinh tế' ? 'selected' : '' %>>Kinh tế</option>
                    <option value="Thiếu nhi" <%= book.category === 'Thiếu nhi' ? 'selected' : '' %>>Thiếu nhi</option>
                </select>
            </div>
        </div>

        <div class="row mb-3">
            <div class="col">
                <label class="form-label">Số lượng tồn kho</label>
                <input type="number" name="unit_in_stock" class="form-control" placeholder="Nhập tồn kho" value="<%= book.unit_in_stock || 0 %>" min="0" step="1" required>
            </div>
            <div class="col">
                <label class="form-label">Giá</label>
                <input type="number" name="price" class="form-control" placeholder="Nhập giá sách" value="<%= book.price || '' %>" min="1" step="0.01" required>
            </div>
        </div>

        <div class="mb-4">
            <label class="form-label">Cập nhật ảnh bìa</label>
            <input type="file" name="coverImage" class="form-control" accept="image/*">
            <% if (book.coverImageUrl) { %>
                <img src="<%= book.coverImageUrl %>" width="90" class="mt-2 img-thumbnail" alt="book-cover">
            <% } %>
        </div>

        <button type="submit" class="btn btn-warning w-100">Cập Nhật</button>
        <a href="/books" class="btn btn-secondary w-100 mt-2">Quay Lại</a>
    </form>
</body>
</html>